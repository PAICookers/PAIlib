## 1

```
paicorelib/neuron_defs.py
@unique
class ThresholdNegMode(IntEnum):
```

此处未做修改，需要和ThresholdPosMode 保持统一，在2.5芯片中加入了天花板模式

## 2

```
paicorelib/neuron_defs.py

@unique
class LeakAddMode(IntEnum):
ram_defs 已有相同定义 LeakDirectionMode
```

此处未做修改，LeakDirectionMode的命名未体现出加法这一关键属性

## 3

```
paicorelib/core_model.py
            or self.add_potential == PotentialAddMode.DIRECT_POTENTIAL
        ):
            if self.weight_width != WeightWidth.WIDTH_1BIT:
                self.weight_width = WeightWidth.WIDTH_1BIT
Member
@KafCoppelia
KafCoppelia
2 hours ago
此处直接raise错误较好，要不然权重参数完全被改了
```

此处未做修改，已经和钟毅交流，在add_potential或max_pooling模式下，权重参数可为任意值，因为这个值不影响芯片的工作。此处默认配置为1bit

## 4

```
paicorelib/neuron_model.py

    @model_validator(mode="after")
    def check_fold_number(self) -> "FoldedNeuronPart1":
        if (
Member

fold_number 含义需确认？
```

已经和钟毅交流，此处值需要相等
